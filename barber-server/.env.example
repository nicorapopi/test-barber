PORT=3000
NODE_ENV=development

# SQLite (default for local dev if DATABASE_URL is empty)
DB_CLIENT=sqlite

# PostgreSQL (for production/Render)
# DB_CLIENT=postgres
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME
